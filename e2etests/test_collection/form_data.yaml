---
method: Post
url: "{{.baseUrl}}"
urlparams:
  foo: bar
  baz: bin
headers:
  Content-Type: multipart/form-data
#   Header is optional when body has "formdata" or "urlencodedformdata"
body:
  formdata:
    user: "{{.userName}}"
    product: "{{.product}}"
    myBool: false
    message1: '{{getValueOf "data.height" "graphql"}}'
    # message: !!bool '{{getValueOf "data.runs" "graphql"}}'
#   Need double quotes for the gotemplate function argument
