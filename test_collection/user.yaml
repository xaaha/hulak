---
method: post
url: "{{.baseUrl}}"
urlparams:
  foo: bar
  baz: bin
headers:
  Content-type: application/json
body:
  # formdata:
  #   foo: bar
  #   baz: bin
  randomThing:
    foo: this is {{.great}} being ignored correctly
    bar: "{{.this}}"
  # this should be invalid or just ignored
  graphql:
    query: |
      query Hello {
        hello(person: { name: {{.userName}}, age: {{.userAge}} })
      }
  # variables:
  #   run: true
